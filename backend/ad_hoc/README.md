# Ad-hoc backend workflows

Independent Python packages for one-off research and scripts. They may reuse small
backend utilities but do not initialize the web application or database.

The first workflow is [Hover supplement research](hover_supplements/README.md).
Run its commands from `backend`, or use the backend directory on your notebook's
Python import path. There is intentionally no plugin registry or shared ad-hoc framework.
